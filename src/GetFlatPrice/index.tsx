import { Button } from '@material-ui/core'
import React from 'react'

const GetFlatPrice = (): JSX.Element => (
  <Button variant='contained' color='primary'>
    Give me a price
  </Button>
)

export default GetFlatPrice
